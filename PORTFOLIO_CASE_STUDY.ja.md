# Superforge — AIが必要な専門スキルを選ぶ、ものづくりのThin Router

## エグゼクティブサマリー

Superforgeは、プロダクト開発の相談を受け取り、必要な専門スキルだけを選ぶオープンソースのAIルーターです。ユーザーが覚えるのは、原則として`superforge`の一語だけ。作りたい結果を普通の言葉で伝えると、AIが作業をSmall・Medium・Largeに分類し、14の専門スキルから中心となる担当を選びます。同じ作業フェーズの修正なら、再入力は不要です。

設計の焦点は、毎回読み込む情報を減らすことにあります。旧ルーターは345行・2,643語あり、ルーティング以外の周辺機能まで本体に抱えていました。再設計後は99行・713語です。行数を71.3%、単語数を73.0%削減しました。この数字はルーター本体の実測値であり、請求額の削減率を示すものではありません。実際の利用量はモデル、キャッシュ、会話履歴、作業内容によって変わります。

「どの手順を、いつ読ませるか」「どこで証拠を求めるか」「何を次の会話へ残すか」を、一つの利用体験として設計しました。構想から公開判定までを扱いながら、入口は軽く保っています。確認できた成果は、ルーター本文の縮小と、動作ルールを自動検査できるようにしたことです。実利用時のトークン量、速度、費用はまだ測定していません。

この事例から判断できるのは、複数の専門手順を維持したまま、入口の情報量と利用者が覚える操作を減らせることです。

| 項目 | 現在の構成 |
|---|---|
| 公開形態 | GitHub上のオープンソースプロジェクト |
| 中央ルーター | 1個、99行・713語 |
| 専門スキル | 14個 |
| 公開README | 5言語 |
| 利用モード | `quick`・`build`・`ship` |
| 配布 | Git clone、Claude Codeプラグイン、Claude Web用ZIP生成 |
| 品質ゲート | ルーターの動作ルール、公開資料、ZIP構造の自動検査 |

## 調査範囲と方法

比較対象は、旧版コミット`d6d0665`と、本資料を含む新版コミットの`skills/superforge/SKILL.md`です。行数は`wc -l`、空白で区切られた語の数は`wc -w`で計測しました。日本語の自然さや実利用時のトークン数を測る指標ではありません。

ルーティングの評価には、旧版の過剰準備例として記録した5ケースを使いました。現在の自動検査は、保存済みの判定結果と期待値の整合性を確認する静的検査です。モデルへ5件を毎回実行するベンチマークではありません。Claude Web向けの配布構造は、2026年9月21日に確認した公式ガイドを基準にしました。

## 入口の肥大化が、小さな作業にも固定費を生んだ

### 専門スキルを増やすほど、入口が重くなっていた

初期のSuperforgeは、ものづくり全体を一つの司令塔から扱う設計でした。ルーティングとモデル選択に加え、状態保存や運用の規則まで本体に集めていました。大きな開発を始めるときには安心感があります。一方、小さなCSS修正や数件の文言変更でもAIが同じ説明を読み込むため、作業の大きさと準備コストが釣り合わない構造でした。

専門スキル名をユーザーに選ばせる方法も考えられます。しかし、`superforge-ui`、`superforge-dev`、`superforge-verify`の使い分けを理解してから相談を始める体験は、案内役を置く意味を弱めます。利用者が知りたいのは内部の担当表より、作りたい結果へどう進めばよいかです。

### 毎回の呼び出しが、同じ説明の再読を生んでいた

旧版の説明は、各メッセージに`superforge`を付ける運用を抑止していませんでした。同じフェーズの細かな修正でも、ルーターを読み直す余地があったということです。関連する専門スキルを複数同時に指定すれば、似た原則を繰り返し読み、判断に使える情報量が重複説明に奪われます。

長い実行ログも別の問題でした。すべての作業を記録すると、次に作業するAIは現在の事実と過去の履歴を見分けにくくなります。保存量が増えるほど安心になるとは限りません。古い説明には、完了済みの作業をもう一度行わせるリスクがあります。

### 複数のAI環境で、同じ使い勝手を保つ必要があった

ローカルのCodexやClaude Codeでは、14の専門スキルを別々のフォルダとして導入できます。一方、Claude Webへアップロードしたルーターは、ローカルにある兄弟スキルを参照できません。単にルーターだけをZIPにしても、選択先の専門スキルが存在しない状態になります。

そこで、ローカルでは独立性を保ち、Webでは一度のアップロードで全体を使える配布構造が必要になりました。

## Thin Routerで、必要な専門性だけを遅れて読む

### ユーザーの入口と、内部の専門性を分離した

再設計後の`superforge`は、必要な経路だけを開くThin Routerです。通常の使い方は次の一文に集約できます。

> 大きな作業フェーズの最初に一度だけ`superforge`と書き、その後の修正は普通の文章で続ける。

ユーザーは専門スキルを指定しません。ルーターは、依頼内容、リポジトリの状態、現在のフェーズを踏まえて作業規模と中心領域を判断するようAIへ指示します。別のスキルを加えるのは、設計から実装、実装から検証へ移るなど、作業段階が変わったときだけです。

```text
ユーザーの依頼
    ↓
99行のThin Router
    ↓
Small  ── その場で対応。追加スキルなし
Medium ── 中心となる専門スキルを1つ
Large  ── 必要な専門スキルを順番に接続
    ↓
証拠を確認して完了 → 必要な場合だけ公開判定
```

<p align="center">
  <img src="./assets/superforge-map.ja.svg" alt="Superforgeが依頼をSmall、Medium、Largeに分類し、必要な専門スキルだけへ接続する流れ" width="100%">
</p>

Smallの作業にLargeの準備を持ち込みません。Largeでも14スキルを一度に読まず、工程の境界ごとに必要なものを接続します。

## ルーター本文を73.0%縮小した

成果として示す数値は、同じ方法で比較できるものに限りました。

| 指標 | 旧版 | Thin Router | 変化 |
|---|---:|---:|---:|
| `SKILL.md`の行数 | 345行 | 99行 | 71.3%減 |
| `wc -w`による単語数 | 2,643語 | 713語 | 73.0%減 |
| 通常の中心スキル数 | 複数になりやすい | 原則1つ | 動作ルールを変更 |
| 小さな作業の文書化 | 一律に発生しやすい | 必要な場合のみ | 動作ルールを変更 |

測定対象は、旧版コミット`d6d0665`にある`skills/superforge/SKILL.md`と、本資料を含む新版コミットの同ファイルです。行数は`wc -l`、空白区切りの語数は`wc -w`で計測しました。

### 請求額の削減率は約束しない

実際のトークン使用量には、会話とツールの状態、選んだ専門スキル、画像などが影響します。そのため「利用料金が必ず何%下がる」とは表現していません。

確認できたのは、入口で読む本文が短くなったことです。設計上、Smallでは追加スキルを読まず、Mediumでは中心スキルを一つにし、同じフェーズではルーターを呼び直しません。価格表を比べる前に、情報の流れそのものを小さくしました。

<p align="center">
  <img src="./assets/superforge-models.ja.svg" alt="作業規模に応じて読み込む情報を段階的に増やすSuperforgeの設計" width="100%">
</p>

### 3つのモードは、進め方を伝える補助線

通常は`superforge`だけで足ります。作業の進め方を明示したい人向けに、3つのモードを用意しました。

| モード | 対象 | 振る舞い |
|---|---|---|
| `superforge quick` | 範囲の小さい修正 | その場で対応し、不要な計画やログを作らない |
| `superforge build` | 新機能や複数ファイルの変更 | 計画し、中心となる専門スキルを選び、実装後に検証する |
| `superforge ship` | 一般公開前の確認 | 完了の証拠を確認し、独立した公開判定を行う |

これらは内部スキルへのコマンドではありません。ユーザーが意図する作業フェーズを、短く伝えるための語彙です。

### 専門スキルは一つを基本にし、必要な境界でだけ増やす

1回の作業では、中心となる専門スキルを一つ選びます。たとえばUIの一式を作るなら`superforge-ui`を中心にします。実装範囲が複数コンポーネントへ広がった時点で`superforge-dev`を接続し、公開直前になって初めて`superforge-verify`と`superforge-ship`を使います。

専門性は維持しています。AIが各手順を読む時期だけを分け、必要な場面で使えるようにしました。

### 記録は「会話の後にも必要か」で決める

すべての小さな修正にドキュメントを作る運用を廃止しました。後続作業の前提になる仕様、引き継ぎに必要な状態、会話後も参照する判断だけを最新情報として残します。

ログも同様です。残すのは失敗、訂正、リリースなど、後から参照する意味がある出来事です。現在の事実をまとめる文書と履歴を分け、次のAIが古い状態を現在の指示と誤認しにくくしました。

### 完了と公開を、別の判断にした

コードが動くことと、一般公開してよいことは同じではありません。Superforgeは次の3段階を分けています。

1. 専門スキルが成果物を作る。
2. `superforge-verify`が、テスト結果や画面、差分などの証拠を確認する。
3. `superforge-ship`が、運用、法務、セキュリティ、公開設定を含めてリリース可否を判断する。

この分離は、「実装が終わったから公開できる」という飛躍を防ぐためのものです。確認していない項目を確認済みとして報告しない規則も、ルーターに組み込んでいます。

## 使い方は、フェーズの最初に一度だけ

### 基本は、フェーズの最初に一度だけ呼び出す

```text
superforge — 初めての利用者が3分で公開まで進めるよう、オンボーディングを作り直して。
```

ルーターは、作業規模を判断して必要な専門スキルを選ぶようAIへ指示します。同じオンボーディングを直している間は、普通の文章で続けます。

```text
前回の条件は維持。空の状態をもっと分かりやすくして、最後にモバイル表示を見せて。
```

ここでは`superforge`を再指定しません。新しい機能に取りかかる、セキュリティ監査を始める、公開判定へ進むといった大きな区切りで改めて呼びます。

### 専門スキル名を知らなくても動く

```text
superforge — ログイン後に他人のデータが見えないか、公開前に確認して。
```

この依頼では、セキュリティ確認を中心に置き、必要に応じて証拠確認と公開判定へ進むようルーターが指示します。ユーザーが`superforge-secure`や`superforge-verify`を覚えている必要はありません。慣れた利用者が専門スキルを直接指定する余地は残していますが、それは近道であって前提ではありません。

### 細かな修正には、ルーターを呼び出さなくてもよい

誤字、一つの余白、単一ファイル内の小さな変更などは、その場で処理します。ルーターを呼ぶ価値があるのは、作業の分け方や品質ゲートを考える必要がある場面です。この境界を明示したこと自体が、Thin Router化の重要な成果です。

## 14の専門スキルを、一つの入口から選ぶ

14スキルは、ものづくりを「考える・作る・確かめる・出す・引き継ぐ」に分けています。

| フェーズ | スキル | 役割 |
|---|---|---|
| 考える | `superforge-brain` | アイデアを広げ、価値のある案を評価する |
| 考える | `superforge-biz` | 市場、価格、事業モデル、顧客獲得を設計する |
| 考える | `superforge-brand` | 名前、言葉、視覚トークン、ブランドの振る舞いを定める |
| 考える | `superforge-roast` | 公開前に弱点を厳しく洗い出す |
| 作る | `superforge-dev` | 複数要素からなる機能を分解し、実装を進める |
| 作る | `superforge-ui` | Web、iOS、AndroidのUIを設計・実装する |
| 作る | `superforge-scroll` | スクロールとカメラを結びつけた映像的な体験を作る |
| 確かめる | `superforge-a11y` | WCAG、支援技術、各プラットフォームの基準を確認する |
| 確かめる | `superforge-test` | 価値のあるテストを選び、テスト駆動で実装する |
| 確かめる | `superforge-debug` | 根本原因を調べ、再発防止の知識を残す |
| 確かめる | `superforge-secure` | 認証、認可、秘密情報、依存関係などを監査する |
| 確かめる | `superforge-verify` | 完了を宣言する前に、実行時の証拠を確認する |
| 出す | `superforge-ship` | 製品を一般公開してよいか判定する |
| 引き継ぐ | `superforge-handoff` | スレッドやAIツールを移る前に現在状態を保存する |

各スキルは独立した入口を持ち、AIは必要な参考資料だけを段階的に読み込みます。中心ルーターには専門手順を複製していません。専門スキルを更新しても、ルーターを肥大化させずに済みます。

## Claude Webでは、14のガイドを一つのZIPへ束ねる

### 一つのZIPに、軽い入口と14の専門ガイドを共存させた

公式ガイドでは、ZIPの最上位にスキル名と一致するフォルダが必要です。`superforge-claude-web.zip`は`superforge/`を一つだけ置き、その中へ中央ルーターと14の専門ガイドを収めます。

```text
superforge-claude-web.zip
└── superforge/
    ├── SKILL.md
    ├── README-WEB.md
    ├── PROVENANCE.md
    ├── references/claude-web.md
    └── specialists/
        ├── superforge-ui/GUIDE.md
        ├── superforge-secure/GUIDE.md
        └── ...14スキル分
```

ZIP内では、専門スキルの入口ファイルを`SKILL.md`から`GUIDE.md`へ改名します。公式のアップロード形状では一つのスキルとして扱われます。選んだ専門ガイドだけを必要時に読むようルーターが指示し、各ガイドの参照資料やスクリプトは同じ相対構造で同梱します。

ローカル専用の動的ワークフローはClaude Webでは実行できないため、ZIPには入れません。各専門スキルに用意された通常手順へ切り替えます。ローカル版と同等の機能があるとはうたわず、使える機能と使えない機能を`README-WEB.md`で明示します。

ZIP生成はPython標準ライブラリだけで再現できます。テスト用の`evals/`、キャッシュ、Git情報、開発中のログは除外し、出典コミットと日付を`PROVENANCE.md`へ記録します。

## ルールと配布構造を自動検査する

### 使い方を文章だけの約束にしない

Thin Routerの動作ルールは`check_superforge_router.py`で静的に検査します。主な確認項目は次のとおりです。

- 本体が100行以内、descriptionが200文字以内である
- 固定モデル一覧を本体へ戻さない
- Small・Medium・Largeと`quick`・`build`・`ship`が定義されている
- 記録条件と完了前の検証が残っている
- 記録済み5ケースの判定と期待値が一致する

5ケースのプロンプトをモデルへ再実行するテストではありません。

公開資料には別の静的検査を用意しました。5言語のREADMEでは、必須のモード、作業規模、専門スキル名が文字列として含まれることを確認します。10枚のSVGに固定モデル名がないこと、ポートフォリオ資料に実測と制約があることも検査対象です。翻訳の意味や文章品質までは判定しません。

生成したWeb版ZIPは、次の項目を機械検査します。

- 最上位フォルダが`superforge/`の一つだけである
- 入口が`superforge/SKILL.md`の一つだけである
- 14個すべての`GUIDE.md`が入っている
- `evals/`、キャッシュ、動的ワークフローを含まない
- ZIP内のパスに上位移動や絶対パスがない
- 出典コミットと日付を記録している

これは、ZIPの形と14個の専門ガイドの入口がそろっていることを確かめる検査です。実際のアップロードと各専門ガイドのプロンプト実行は、今後の受け入れテストとして残っています。

### 多言語と図版を同じ説明ルールから更新する

GitHubの入口は英語、日本語、スペイン語、韓国語、簡体字中国語の5言語です。各READMEは同じ章構成を持ち、日本語は英語の語順をなぞらず、利用場面から理解できる文章へ書き直しました。

図版は2種類×5言語の計10枚です。既存URLを壊さないためファイル名は維持し、内容を更新しました。1枚目はフェーズ開始から追加指示までの流れ、2枚目はSmall・Medium・Largeごとの情報読み込みを示します。SVGは一つの生成スクリプトから出力するため、言語間でレイアウトや内容の版が食い違いません。

## 静的検査だけでは、実利用の効果までは証明できない

### Thin Routerでも、専門ガイドそのものは短くならない

セキュリティ監査やアクセシビリティ確認には相応の手順が必要です。専門性は削らず、不要な場面で専門手順を読む回数を減らしました。Largeの仕事では複数のガイドが必要になるため、AIに読み込ませる情報量はSmallより増えます。

### 自動ルーティングは、依頼の曖昧さを完全には消せない

「いい感じにして」のように目的が広い依頼では、AIが前提を確認する場合があります。Thin Routerは専門スキル名を尋ねる負担を減らしますが、製品上の重要な選択まで勝手に確定する仕組みではありません。

### Web版は、一つのZIPにまとめるぶん配布サイズが増える

ローカル版は必要なスキルを独立して管理できます。Claude Web版は一度のアップロードを優先し、14の専門ガイドと参照資料を同梱します。ZIPのファイルサイズは増えますが、AIが毎回すべてを読むわけではありません。選択されたガイドだけを読むよう、ルーターが指示します。

### 外部サービスの仕様は変わる

アップロード画面、各AIツールのスキル探索場所、モデル名や料金は将来変わります。公開説明では固定モデル名をルーティングの中心に置きません。配布構造は公式ガイドに合わせ、変更を自動検査で検知できるようにしました。それでも、外部仕様の変更に完全には自動追従できません。

### 成果の範囲

この資料が示すのは、リポジトリの構造、ルーターの縮小、配布物、自動検査できる動作ルールです。利用者全体の平均トークン削減率、開発速度、売上への影響は現時点では測定していません。Claude Webへの実アップロードと、アップロード後の自動ルーティングも未確認です。今後は匿名の実行計測を任意で導入し、Small・Medium・Largeごとの読み込み量と完了時間を比較します。

## 実装範囲と担当

設計範囲は、プロダクトコンセプト、情報設計、ルーティングの動作ルール、専門スキル体系、配布方式です。実装範囲には、多言語ドキュメント、SVG図版、検証スクリプト、GitHub公開資料が含まれます。

AIは実装とレビューに活用しました。構造の採否、削る対象、利用者との約束、測定値の見せ方は私が判断しました。

特に重要だったのは、旧版の何を外すかという判断です。利用モデルの一覧、常時ログ、常時ドキュメント生成、毎回の再ルーティングを中央ルーターから外し、必要なときだけ参照する専門スキル側へ移しました。

## 次は実利用データとWeb受け入れテストを加える

1. 利用環境が提供する使用量ログを用い、再設計前後を同じタスク群で比較する。
2. 14の専門スキルについて、Web版で参照できないローカル専用機能をさらに明示する。
3. 多言語READMEの意味差分を自動検知し、説明ルールのずれを見つける。
4. GitHub ReleaseへClaude Web用ZIPとチェックサムを自動添付する。
5. Small・Medium・Largeの判定例を実利用から追加し、迷いやすい境界を改善する。

## ポートフォリオ掲載用コピー

### 一覧カード

**タイトル**

Superforge — AIが必要な専門スキルを選ぶThin Router

**短い説明**

ものづくりを14の専門スキルに分け、作業規模に応じて必要な手順だけを読むAIルーター。ユーザーはフェーズの最初に`superforge`と書くだけで、専門スキルの選択をAIに任せられます。

**タグ**

AI Product / Developer Tools / System Architecture

### ヒーロー

**見出し**

専門家を増やしても、入口は一つ。

**リード**

Superforgeは、戦略・デザイン・実装・検証・公開を14の専門スキルへ分けながら、使い方を`superforge`の一語にまとめたオープンソースのAIルーターです。345行あった中央ルーターを99行へ再設計し、小さな修正では重い準備を省けるようにしました。

### Before / After

**Before — すべてを毎回読む司令塔**

専門スキルが増えるほど、ルーター、モデル表、ログ、保存規則が膨らみました。小さな修正にも同じ準備が必要になる構造でした。

**After — 必要な経路だけを開くThin Router**

ルーターは、Small・Medium・Largeを判断して中心となる専門スキルを原則一つ選ぶようAIへ指示します。同じフェーズの追加指示ではルーターを呼び直しません。検証と公開判定は、その段階に進んだときだけ行います。

### 数字で示す成果

- 345行から99行へ。中央ルーターを71.3%縮小
- 2,643語から713語へ。ルーター本文を73.0%縮小
- 14の専門スキルを、一つの入口から選ぶルーティング表
- 5言語のREADMEと10枚のSVGを、同じ説明ルールで公開
- Claude Webの公式アップロード形状に合わせ、14の専門ガイドを一つのZIPとして生成

注記：実利用時のトークン削減率と、Claude Webへの実アップロードは未確認です。

### 締めの一文

> AIにすべてを読ませるのではなく、今必要な専門性だけを渡す。

## 再現方法と参考資料

実測は次のコマンドで再現できます。

```bash
git show d6d0665:skills/superforge/SKILL.md | wc -l -w
git show HEAD:skills/superforge/SKILL.md | wc -l -w
python3 scripts/check_superforge_router.py
python3 scripts/generate_public_diagrams.py
python3 scripts/package_skills.py --claude-web
python3 scripts/check_public_release.py
```

- リポジトリ: <https://github.com/takaoumehara/superforge-skill>
- Claudeのカスタムスキル作成ガイド: <https://support.claude.com/en/articles/12512198-how-to-create-custom-skills>
- Claudeでのスキル利用ガイド: <https://support.claude.com/en/articles/12512180-use-skills-in-claude>

本資料の数値は、2026年9月21日時点のリポジトリで確認したものです。

Superforgeの成果は、指示を増やしたことではありません。専門性を保ちながら、今の作業に不要な指示を読ませない入口を作ったことです。
